{
  "name": "fither-ai",
  "private": true,
  "version": "0.1.0",
  "description": "FitHer AI — adaptive AI workout coach + beginner-vetted gym discovery for Thai women (LINE + web).",
  "workspaces": ["server", "web"],
  "engines": { "node": ">=20" },
  "scripts": {
    "dev:local": "npm run -w server dev",
    "dev:web": "npm run -w web dev",
    "build": "npm run -w web build",
    "test": "npm run -w server test",
    "seed": "npm run -w server seed",
    "demo:reset": "npm run -w server demo:reset",
    "sim": "npm run -w server sim",
    "deploy:worker": "npm run -w server deploy"
  }
}
